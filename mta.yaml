_schema-version: 3.3.0
ID: purchase-order-app
description: A simple CAP project.
version: 1.0.0
modules:
- name: purchase-order-app-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: purchase-order-app-auth
  - name: purchase-order-app-db
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
    instances: 1
  build-parameters:
    builder: npm-ci
- name: purchase-order-app-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: purchase-order-app-db
  parameters:
    buildpack: nodejs_buildpack
- name: purchase-order-app-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: purchase-order-app-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - compoapppolist.zip
      name: compoapppolist
      target-path: resources/
- name: compoapppolist
  type: html5
  path: app/po-list
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: purchase-order-app-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      oauth2-configuration:
        credential-types:
        - binding-secret
        - x509
      tenant-mode: dedicated
      xsappname: purchase-order-app-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: purchase-order-app-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: purchase-order-app-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: purchase-order-app-html5-service
    service-plan: app-host
- name: purchase-order-app-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: purchase-order-app-destination-service
    service-plan: lite
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npm ci
    - npx cds build --production
